[project]
name = "dabor"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "pynacl",
    "discohook @ git+https://github.com/jnsougata/discohook.git@dev",
    "httpx"
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk"
]
