[project]
name = "dabor"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "pynacl",
    "discohook",
    "httpx"
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk"
]
